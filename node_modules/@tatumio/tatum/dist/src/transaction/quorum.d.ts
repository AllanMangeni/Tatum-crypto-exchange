import { CreateRecord } from '../model';
import { TransferQuorum } from '../model';
/**
 * Send Quorum store data transaction to the blockchain.
 * This operation is irreversible.
 * @param body content of the transaction to broadcast
 * @param provider url of the Quorum Server to connect to.
 * @returns transaction id of the transaction in the blockchain
 */
export declare const sendStoreDataQuorumTransaction: (body: CreateRecord, provider: string) => Promise<{
    txId: any;
}>;
/**
 * Send Quorum transaction to the blockchain.
 * This operation is irreversible.
 * @param body content of the transaction to broadcast
 * @param provider url of the Quorum Server to connect to.
 * @returns transaction id of the transaction in the blockchain
 */
export declare const sendQuorumTransaction: (body: TransferQuorum, provider: string) => Promise<{
    txId: any;
}>;
